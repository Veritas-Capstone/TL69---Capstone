"""DEPRECATED package: `api.core` moved to `api` subpackages such as
`api.webscraping` and `api.claim_verification`.

Keep this module only as a short-lived compatibility layer. Remove once
you're confident no code imports from `api.core`.
"""
